name: WaybackRecon Pipeline

on:
  workflow_dispatch:
      inputs:
            domain:
                    description: 'Single domain (e.g. example.com)'
                            required: false
                                    default: ''
                                    
